---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { useTranslations } from '../../i18n/utils';

const lang = Astro.currentLocale ?? 'es';
const t = useTranslations(lang);
---

<BaseLayout title={t.meta.privacidad.title} description={t.meta.privacidad.description} noindex>
  <section class="section legal">
    <h1>{t.privacidad.h1}</h1>
    <p class="draft-note">
      {t.privacidad.draftNote}
    </p>
    <p>
      {t.privacidad.body}
    </p>
    <p>{t.privacidad.responsable} <a href="mailto:vanessa.cal.sua@gmail.com">vanessa.cal.sua@gmail.com</a></p>
  </section>
</BaseLayout>

<style>
  .legal {
    max-width: 72ch;
    padding-top: clamp(140px, 20vh, 200px);
  }
  .legal h1 {
    font-size: clamp(2rem, 4vw, 3rem);
    margin-bottom: 24px;
  }
  .legal p {
    font-size: 16px;
    line-height: 1.7;
    color: var(--body-copy);
    margin-bottom: 16px;
  }
  .draft-note {
    background: var(--panel);
    padding: 16px 20px;
    border-radius: 12px;
    font-size: 14px;
    color: var(--muted-3);
  }
</style>
